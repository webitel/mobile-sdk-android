package com.webitel.mobile_sdk.domain


/**
 * Specifies the logging level for outputting messages.
 * Controls the verbosity of log messages in the application, from the most detailed (DEBUG) to no logging (OFF).
 */
enum class LogLevel {
    DEBUG, INFO, WARN, ERROR, OFF
}